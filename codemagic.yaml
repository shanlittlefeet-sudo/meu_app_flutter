workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - cd cart_app && flutter pub get
      - cd cart_app && flutter build ios --release --no-codesign
    artifacts:
      - cart_app/build/ios/iphoneos/*.ipa
